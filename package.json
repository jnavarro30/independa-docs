{
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "gh-deploy": "git push origin :gh-pages && git subtree push --prefix .vitepress/dist origin gh-pages",
    "gh-deploy-init": "git push origin && git subtree push --prefix .vitepress/dist origin gh-pages"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
